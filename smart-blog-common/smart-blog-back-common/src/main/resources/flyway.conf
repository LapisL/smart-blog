flyway.placeholders.commonColumns=create_date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, created_by CHAR(36) NOT NULL DEFAULT 'system',update_date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, updeted_by CHAR(36) NOT NULL DEFAULT 'system', version bogint NULL DEFAULT 0
flyway.jarDir=